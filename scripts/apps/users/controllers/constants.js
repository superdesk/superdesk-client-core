/**
  * Values and labels for types of users
 */
export const USER_TYPES = Object.freeze([
    {label: gettext('User'), value: 'user'},
    {label: gettext('Author'), value: 'author'},
    {label: gettext('Administrator'), value: 'administrator'}
]);
